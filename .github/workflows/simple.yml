name: Shell commands

on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: write a string
        run: ecxho "Hello Github Actions"
      - name: multiline script
        run: |
          node -v
          npm -v
